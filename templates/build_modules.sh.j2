#!/bin/bash

set -e

./configure {% for param in nginx_core_configure %} {{param}} {% endfor %} {% for module in nginx_modules %} --add-dynamic-module=../{{module.name}}-module {% endfor %}

make modules

cp objs/*.so {{result_dir}}
